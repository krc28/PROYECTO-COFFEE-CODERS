import React from 'react';
import Icono from 'media/IconoVentas.png';

const IconoVentas = () => {
  return <img className='rounded-full flex items-center justify-center mx-auto h-40 w-auto' src={Icono} alt='Workflow' />;
};

export default IconoVentas;